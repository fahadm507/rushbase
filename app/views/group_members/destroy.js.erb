target = $("#group-join-<%=@group_member.group.id%>")
target.html("<%= j(render partial: 'group_members/create_form',
  locals: {
    group: @group_member.group,
    group_member: @group_member
  })%>")
