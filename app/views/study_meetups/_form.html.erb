<div class="new-meetup-form" id="new-meetup-form-id">
  <%= bootstrap_form_for(@study_meetup) do |f| %>
    <% if @study_meetup.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@study_meetup.errors.count, "error") %> prohibited this study_meetup from being saved:</h2>

        <ul>
        <% @study_meetup.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class="field">
      <%= f.text_field :name %>
    </div>
    <div class="field">
      <%= f.text_area :description %>
    </div>

    <div class="field">
      <%= f.text_area :goal %>
    </div>
      <div class="actions">
        <%= f.submit "create meetup", class: "btn btn-primary" %>
      </div>
  <% end %>
</div>
