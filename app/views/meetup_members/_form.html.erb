
<% if current_user && GroupMember.member?(current_user, group)%>
  <%= render partial: 'group_members/destroy_form', locals: {group: group}%>
<% else %>
  <%= render partial: 'group_members/create_form', locals: {group: group}%>
<%end%>
