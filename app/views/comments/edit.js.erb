var target = $("ul#comment-for-post-<%= @comment.group_post.id %> #comment-<%= @comment.id%>")
target.html("<%= j(render partial: 'comments/form',
  locals: {
    group_post: @comment.group_post,
    group: @comment.group_post.group,
    comment: @comment
  })%>");
