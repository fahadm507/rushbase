<div class="row edit form">
  <div class="group-header panel-heading row">
    <%= bootstrap_form_for([group, comment], remote: true) do |f| %>
      <% if comment.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

          <ul>
          <%comment.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
          </ul>
        </div>
      <% end %>

    <%= f.text_area :description, label: { text: "Edit description"}, class: "group-body panel-body", contenteditable: true %>
      <div class="actions">
        <%= f.submit "Done", class: "btn btn-success" %>
      </div>
    <% end %>
  </div>
</div>
<hr>
