var target = $(".comments-details ul#comment-for-post-<%= @comment.group_post.id %>")
target.html("<%= j(render partial: 'comments/comment',
  locals: {
    user: @comment.user,
    post: @comment.group_post,
    comment: @comment
  })%>");

