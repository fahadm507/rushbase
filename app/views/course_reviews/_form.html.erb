
<div class="container">
  <%= bootstrap_form_for([@course, @course_review]) do |f| %>
    <% if @course_review.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@course_review.errors.count, "error") %> prohibited this course_review from being saved:</h2>

        <ul>
        <% @course_review.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>
    <br>

    <div class="field">
      <%= f.text_field :title %>
    </div>
    <div class="field">
      <%= f.text_area :description %>
    </div>
    <div class="field">

    </div>
      <div class="actions">
        <%= f.submit %>
      </div>
  <% end %>
</div>
