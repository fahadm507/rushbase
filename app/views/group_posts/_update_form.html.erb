<div class="row edit form">
  <div class="post-header panel-heading row">
    <%= bootstrap_form_for([group, group_post], remote: true) do |f| %>
      <% if @group_post.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@group_post.errors.count, "error") %> prohibited this group_post from being saved:</h2>

          <ul>
          <% @group_post.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
          </ul>
        </div>
      <% end %>

    <%= f.text_area :description, label: { text: "Edit description"}, class: "post-body panel-body" %>
      <div class="actions">
        <%= f.submit "Update", class: "btn btn-success" %>
        <span>
        <%=link_to "Cancel", group_group_post_path(@group_post.group, @group_post), remote: true, class: "btn btn-default" %>
        </span>
      </div>
    <% end %>
  </div>
</div>
