<span id="upvote-btn-<%=post.id%>">
  <%=link_to(group_group_post_upvote_path(group,post.id, Upvote.where(user_id: current_user, group_post_id: post).first),remote: true, method: :delete) do %>
    <i class="glyphicon glyphicon-thumbs-up"></i>
  <%end%>
</span>
