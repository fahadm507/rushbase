<%= form_for(@current_course) do |f| %>
  <% if @current_course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@current_course.errors.count, "error") %> prohibited this current_course from being saved:</h2>

      <ul>
      <% @current_course.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
