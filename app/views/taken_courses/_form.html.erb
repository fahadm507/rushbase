<%= form_for(@taken_course) do |f| %>
  <% if @taken_course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@taken_course.errors.count, "error") %> prohibited this taken_course from being saved:</h2>

      <ul>
      <% @taken_course.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
